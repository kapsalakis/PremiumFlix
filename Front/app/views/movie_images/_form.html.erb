<%= form_for(movie_image,  html: { multipart: true }) do |f| %>
  <% if movie_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(movie_image.errors.count, "error") %> prohibited this movie_image from being saved:</h2>

      <ul>
      <% movie_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :movie_id %>
    <%= f.text_field :movie_id %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
